<%- include('../../partials/dashboard') %>

    <main class="flex-grow p-4 md:p-8 lg:p-12 bg-gray-50">
        <!-- Flash Messages -->
        <%- include('../../partials/flashMessage') %>

            <!-- Header Section -->
            <% if (student) { %>
                <header
                    class="flex items-center justify-between bg-white rounded-xl shadow-lg p-6 mb-8 border border-gray-100">
                    <h2 class="text-2xl sm:text-3xl font-semibold tracking-wide text-gray-800">Attendance Details for
                        <%= student.name %>
                    </h2>
                </header>
                <% } else { %>
                    <header
                        class="flex items-center justify-between bg-gradient-to-r from-red-500 to-red-600 text-white rounded-xl shadow-lg p-6 mb-8">
                        <h2 class="text-2xl sm:text-3xl font-semibold tracking-wide">Student Not Found</h2>
                    </header>
                    <% } %>

                        <% if (attendanceRecords.length> 0) { %>
                            <!-- Attendance Summary -->
                            <div class="bg-white rounded-xl shadow-lg p-6 mb-8 border border-gray-100">
                                <h3 class="text-xl font-semibold text-gray-800 mb-4">Attendance Summary</h3>
                                <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Class</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= student ? student.stdClass.name : 'N/A' %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Section</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= student ? student.stdClass.section : 'N/A' %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Total Days</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= totalDays %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Present Days</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= presentDays %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Absent Days</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= absentDays %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Leave Days</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= leaveDays %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Late Days</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= lateDays %>
                                        </p>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-lg shadow-sm">
                                        <p class="text-sm text-gray-500">Attendance Percentage</p>
                                        <p class="text-lg font-medium text-gray-800">
                                            <%= attendancePercentage %>%
                                        </p>
                                    </div>
                                </div>
                            </div>

                            <!-- Date Range Filter -->
                            <div class="bg-white rounded-xl shadow-lg p-6 mb-8 border border-gray-100">
                                <form action="/dashboard/attendance/checkattendance" method="GET"
                                    class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                                    <div class="flex flex-col">
                                        <label for="fromDate" class="text-sm font-medium text-gray-600">From
                                            Date</label>
                                        <input type="date" name="fromDate" value="<%= searchQuery.fromDate %>"
                                            class="px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                                    </div>
                                    <div class="flex flex-col">
                                        <label for="toDate" class="text-sm font-medium text-gray-600">To Date</label>
                                        <input type="date" name="toDate" value="<%= searchQuery.toDate %>"
                                            class="px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                                    </div>
                                    <button type="submit"
                                        class="self-end px-6 py-2 bg-blue-600 text-white rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                                        Filter
                                    </button>
                                </form>
                            </div>

                            <!-- Daily Attendance Table -->
                            <div class="bg-white rounded-xl shadow-lg p-6 border border-gray-100">
                                <h3 class="text-xl font-semibold text-gray-800 mb-4">Daily Attendance</h3>
                                <div class="overflow-x-auto">
                                    <table class="min-w-full table-auto border-collapse">
                                        <thead class="bg-gray-50">
                                            <tr>
                                                <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">Date
                                                </th>
                                                <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">Class
                                                </th>
                                                <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">Status
                                                </th>
                                                <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">
                                                    Remarks</th>
                                            </tr>
                                        </thead>
                                        <tbody class="text-gray-700">
                                            <% attendanceRecords.forEach(attendance=> { %>
                                                <tr class="border-b hover:bg-gray-50 transition duration-300">
                                                    <td class="py-4 px-4">
                                                        <%= attendance.date ? attendance.date.toLocaleDateString()
                                                            : 'Invalid Date' %>
                                                    </td>
                                                    <td class="py-4 px-4">
                                                        <%= attendance.classId.name %>
                                                    </td>
                                                    <td class="py-4 px-4">
                                                        <span class="px-2 py-1 rounded-full text-sm font-medium
                                        <%= attendance.status === 'Present' ? 'bg-green-100 text-green-600' :
                                            attendance.status === 'Absent' ? 'bg-red-100 text-red-600' :
                                            attendance.status === 'Late' ? 'bg-yellow-100 text-yellow-600' :
                                            'bg-blue-100 text-blue-600' %>">
                                                            <%= attendance.status %>
                                                        </span>
                                                    </td>
                                                    <td class="py-4 px-4">
                                                        <%= attendance.remarks || 'N/A' %>
                                                    </td>
                                                </tr>
                                                <% }) %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            <% } else { %>
                                <!-- No Records Found -->
                                <div class="flex flex-col items-center justify-center text-center py-10">
                                    <p
                                        class="text-xl font-semibold text-gray-700 bg-gray-100 px-6 py-3 rounded-lg shadow-md">
                                        No Attendance Record Found.
                                    </p>
                                </div>
                                <% } %>
    </main>

    <%- include('../../partials/dashboardfooter') %>